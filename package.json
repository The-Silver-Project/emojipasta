{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^2.4.0"
  }
}
